import path from 'path'

export default function getMonorepoNodeModules() {
  const isMonoRepo = /\/resolve$/.test(path.resolve(__dirname, '../../..'))

  if (isMonoRepo) {
    return [path.resolve(__dirname, '../../../node_modules')]
  } else {
    return []
  }
}
